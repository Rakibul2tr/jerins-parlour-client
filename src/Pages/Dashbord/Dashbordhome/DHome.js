import { Typography } from '@mui/material';
import React from 'react';

const DHome = () => {
    return (
        <div>
            <Typography variant="h3">Wielcome for site Admin</Typography>
        </div>
    );
};

export default DHome;